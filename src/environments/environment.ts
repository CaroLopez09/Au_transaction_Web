export const environment = {
  production: true,
  apiBaseUrl: '/api',
  /** Herramientas solo del sandbox del proveedor (simular depósitos). El BFF igualmente responde 422 fuera del sandbox (G-18). */
  sandboxTools: false,
};
